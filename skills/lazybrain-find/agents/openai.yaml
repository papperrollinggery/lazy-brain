interface:
  display_name: "LazyBrain Local Lookup"
  short_description: "Find local capability sources beyond the host catalog"
  brand_color: "#7C3AED"
  default_prompt: "Use $lazybrain-find to find a local capability source when the current Codex catalog does not resolve my lookup."
